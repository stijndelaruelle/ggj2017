using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;

public static class StringExtension
{
	//public static string Shuffle(this string source)
	//{
	//	string shuffledString = "";
	//	List<char> sourceChars = source.ToCharArray().ToList();

	//	while(sourceChars.Count > 0)
	//	{
	//		shuffledString.Insert(0, sourceChars[Random.Range(0, sourceChars.Count)]);
	//	}

	//	char[] array = source.ToCharArray();
	//	Random rng = new Random();
	//	int n = array.Length;
	//	while (n > 1)
	//	{
	//		n--;
	//		int k = rng.Next(n + 1);
	//		var value = array[k];
	//		array[k] = array[n];
	//		array[n] = value;
	//	}

	//	return new string(array);
	//}
}
